# 三猫书桌 · Phase1a 综合裁决与 Phase1b 计划

> **2026-08-12** | 方法：7-agent 全面审查（交付审计 + 留存度量就绪 + 猫 hook 三视角对抗评估 + 用户验证方案 + 综合裁决）。抽查核对过代码（`database.dart:46-53`、`fsrs.dart:120/164-169`、`cat_provider.dart:11/84-90`、`review_provider.dart:122`、`main.dart:37-50`）。

## 1. Phase1a 裁决：合格 ship，带一颗必须拆掉的雷

项目历史上第一次真实交付（Web Pages + APK + CI），23/23 测试绿。**猫养成不是留口**——`review_provider.dart:122` 评分成功 → `onCardReviewed()` → intimacy+1，带重入守卫，wiring 真实接通。"只增不扣、不区分档位"是写明的设计决策（`cat_provider.dart:7-12`），不是偷懒。触点三处（首屏问候/翻卡顶栏/完成页）布局正确。

### 必修 bug（按致命度）

| 级别 | 问题 | 位置 | 为何必修 |
|---|---|---|---|
| **P0** | **getDueCards 时间过滤丢卡** | `lib/core/db/database.dart:50` vs `fsrs.dart:120,164` | 今天14:30评good→due明天14:30；明天10:00打开队列空。**实验要测的"次日打开"行为被工具自己掐死**。更糟 `database.dart:46` 注释自称"按天对齐"而实现没有。同病：`deck_provider.dart:18-22` 今日到期数 |
| **P0** | **发出去无法算次日留存** | `cloud_sync.dart` 只同步 cards，无 app-open 埋点 | 云端没有任何"用户某天打开了App"的信号。唯一带时间戳的事件是 cards.updated_at（=复习了卡，非打开了App）。**不发没法度量=重蹈元根因**。且匿名用户(本 App 不强制登录)在云侧零脚印，是最大盲区 |
| **P1** | 猫进度与 auth 脱钩、唯一不上云 | `lib/features/cat/`（grep userId 零命中）| `cat_provider.dart:11` 写明 1a 有意豁免——可接受，但次留 hook 核心资产(猫)是全系统唯一无云备份状态。用户投入数周 intimacy 一朝归零 = 依恋 hook 永久击杀 |
| P2 | intimacy 落盘 fire-and-forget | `review_provider.dart:122` 未 await | 低优先，10人14天不影响结论，可接受 |
| P2 | bootstrap 不 await 猫 `_load()` | `main.dart:37-50` | 老用户首屏闪"亲密度0·困困"再跳变。loading 占位半小时可修 |

**测试债**：`cat_provider_test` 只覆盖纯函数——删掉 `review_provider.dart:122` 测试仍全绿，核心 hook 链路无保护。

## 2. 猫 hook 强度裁决（三视角对抗评估）

**Finch=uncertain / Forest=uncertain / 收集=unlikely。机制层诚实结论：当前猫是"session 内优秀的正反馈装饰，跨日驱动力接近零"。**

三份独立评估罕见收敛到同一组事实：
- **Day2 与 Day1 零差异**：无 streak、无通知、无回归迎接、无成长形态。完成页"明天再来看看它吧"是不被机制兑现的承诺。
- **进度曲线首日封顶**：mood 阈值 0/1/3/8/20，第一 session 约20张卡烧完5档，此后永远 encouraging；`level` 算了但全 App 未渲染。
- **零拉力通道**：App 关闭后猫不存在，D2 决策瞬间没有介入通道。

- **最可能成功**：FSRS 到期卡的欠债感 + 考研人群自带强外驱力——可能靠工具性撞过 30%，但功劳不在猫。
- **最危险的不是 hook 弱，是归因混淆**：即使次留≥30%，也无法证明是"猫+考研"成立——"晒猫截图+访谈对账"是唯一补救。

**⚠ 裁决：发用户前不补任何 hook 机制（通知/streak/配饰一律不加）。** 理由：①三视角判定是机制推演不是数据，考研强外驱人群可能证伪；②本轮实验就是测"最小猫的裸强度"，先加机制就永远不知道裸强度；③元根因是范围蔓延——"再补个小功能"正是永动机的启动按钮。**但度量埋点不是功能，是观测仪器，必须加。**

## 3. 发用户前必做清单（约 1.5-2 天，做完即止）

准入标准：**不做就得不到可信的次留数字，或会永久击杀用户资产**。

1. **修 getDueCards 按天对齐**（P0，半天）。`database.dart:50` end 对齐当天 23:59:59（或改用与 `FsrsCard.isDue` 一致的按天比较）；`deck_provider.dart:18-22` 同修；补"今天到期今天任何时刻都算、明天到期不算"边界测试。
2. **建 `user_daily_activity` 表 + 冷启动 upsert**（P0，半天~1天）。`(user_id, day, first_opened_at, last_opened_at, open_count, PK(user_id,day))`，RLS 限 `auth.uid()`；`main.dart:_bootstrap` 登录态恢复后 upsert 一行。这是 DAU/次留唯一可算来源。
3. **猫 key 按 userId 命名空间 + intimacy 顺手 PUSH 一个整数**（P1，随#2做，零额外网络）。SharedPreferences key 加 userId 前缀防换号串数据；`user_daily_activity` 行加一列 `intimacy`，upsert 带上。只做 PUSH 不做 PULL/合并，不建猫专表。
4. **补 review→cat wiring 测试**（随#1，半小时）。grade 后断言 intimacy+1。
5. **次留 SQL 写成文件存 repo**（零代码）。次留 = 存在 `day=trunc(created_at)+1` 活动行的去重 user_id / 注册日去重 user_id；D3 起每天跑一次。

**明确不做（发用户前）**：通知、streak、回归迎接、配饰、mood 阈值重排、UI 统一、猫上云 PULL、bootstrap await 猫（可留 loading 占位）。

**匿名登录裁决**：降级为预案——10 人样本内入组者都是注册用户，匿名黑洞实验期不存在。**若注册漏斗掉点 >50%，立即切 signInAnonymously 先行 + 事后升级邮箱**；否则不动，顺手把注册摩擦当漏斗数据测了。

## 4. Phase1b 计划（两分支，D14 数据定走向）

### 4a 次留 ≥30%（验证通过）：放大已验证 hook，做且只做 3 件（全部复用现有数据基建，零外部美术）
1. **Streak 显形四件套**（约1天）：`cat_provider` 加 streak/lastActiveDate（断档只断 streak 不动 intimacy，严守无惩罚哲学）；猫条显"连续 N 天"徽标；完成页改"明天再来就是连续第 N+1 天"；Day2 首屏显"连续第 N 天，猫咪等你一早就来了"。配每周1次 streak freeze（"猫咪帮你保住了连续"）。
2. **每日本地通知 + 回归迎接**（约1天）：`flutter_local_notifications` 用户自选时间默认晚7点，猫第一人称关怀文案；新一天首启 PixelCat 大尺寸迎接 2 秒。D2 决策瞬间唯一介入通道。
3. **成长曲线重排到跨天尺度**（约半天）：mood 阈值 0/1/3/8/20 → 约 0/20/80/300，猫画3档形态（幼猫→少年→成年，复用 _PixelCatPainter 程序化像素），把已存在却未渲染的 level 显示出来。随机化眨眼间隔（现固定 1.6s 机械感强）。

**不做**：配饰/收集（unlikely+美术阻塞）、撸猫/喂猫（押后）。**归因补强**：埋"首屏问候曝光→进入复习"漏斗，配合访谈对账，回答"≥30% 里猫出了多少力"。

### 4b 次留 <30%（验证失败）：不加功能，三选一纠偏，每轮 14 天
1. **改 hook**：若"回来是为复习，猫是装饰"→ 猫降级为陪伴装饰，streak 直接挂复习行为（不依赖拟人依恋），资源转向词书内容+复习体验。
2. **改人群**：若"墨墨/Anki 重度用户不可能迁移"→ 招募筛的"每天已有复习习惯"恰好选了迁移成本最高人群。换 2027 届早期备考生（未被 Anki 锁定、习惯未成型），猫+低门槛对他们可能是入场理由。同一 App 重测 10 人。
3. **改定位**：若"背单词养猫"整体不成立 → 猫从宠物改定位"学习搭子"（双向关怀：猫问用户今天怎么样——Finch 视角零分项、考研孤独痛点正面解）。最小验证动作=改招募文案/落地页 A/B，**不重写 App**。

**铁律（写进日历）**：<30% 只选一条试，每轮14天，仍不达标继续砍；任何"顺手加个功能"的提议直接视为元根因复发。

## 5. 元根因对账

**这次 ship 打破了"永动机功能工厂"约一半——生成与交付的断裂第一次接上，但结构债仍在。**

**已打破**：①"从不 ship"被打破（Web+APK+CI 真实上线，0→1）②"没有失败判据"被打破（次留<30%砍功能第一次写成铁律+配套 SQL/14天日历/10人招募）③"只听好话"被制度性防御（第三方归因+匿名表单+think-aloud+行为对账）④"不做什么"开始有决策记录（cat_provider.dart:11 写明 1a 不做云同步猫及理由）。

**剩下的结构债**：
- **CEO 仍是唯一交付阻塞点，实验期会加重**——招人/教人/收截图/访谈/跑SQL/D14决策全在一人链路。唯一结构性缓解：观测阻塞点被拆（数据自动落 Supabase，CEO 只看一条 SQL），这个模式要固化成制度。
- **"生成快于验证"病根仍在**：`database.dart:46` 注释自称"按天对齐"而实现没有——AI 生成代码的病：注释比代码先"完成"。**结构性解法：把"度量在线才算 done"写进 Definition of Done**（Phase1a 上线时没有任何 app-open 埋点，说明"ship"定义里还缺"可观测"这一半）。
- **铁律尚未被执行过**：写下来≠做到。真正的考验是 D14 数据出来那一刻——若 <30%，能否真的不加功能。D14 之后才能销账。

## 6. 一句话给 CEO 的建议

**用一天半修掉到期过滤和留存埋点（§3 五条，做完立刻停手），然后把 App 交给 10 个真考研人——从今天起你唯一的新 KPI 不是写代码，而是在 D14 那天看着 SQL 结果执行铁律。**

---

## 附录：用户验证执行方案（14天）

**招人（目标10人）**：正在备考、且每天已在背词/刷卡的 2026/2027 届（非你亲密好友）。渠道：考研QQ/微信群 + 小红书"考研搭子/打卡"超话 + 知乎私信 + 高校自习室贴码 + 朋友二度转发。话术去营销化："帮一个独立开发者做最真实的测试，不刷好评，骂也行"。钩子=学习诊断+资料包（非现金，现金招来薅羊毛的污染数据）。筛掉无复习习惯的（3天打鱼的拉不出次留，污染分母）。**入组硬门槛：注册→导入词书→完成≥5张卡**才算入组。

**要收数据**（决策只看行为，不听嘴上）：
- **北极星·次日留存 D1**（铁律<30%砍）：SQL 按 user_id 把 cards.updated_at 去重到日，有≥1个后续不同日即回访；10人≥3人回访即达标。
- D3/D7 留存（判断一次性好奇 vs 真习惯）。
- 每日复习量（回访者只刷1张=猫没接住，只是礼貌性打开）。
- 激活漏斗（链接→注册→导入→首5张，哪步掉最多决定先修哪）。
- 猫亲密度（本地不上云→靠第3/7天"晒猫截图"收，回答"用户有没有为猫回来"）。
- 回访归因（访谈+截图交叉验证：回来是为猫还是复习刚需）。
- 主观净推值 NPS（0-10，只作参考不作决策）。

**访谈（行为锚定，不问观点）**：
- "带我过一遍你昨天怎么用的？从解锁手机那一刻说起"（看真实路径）。
- "第二天出于什么打开？当时想什么？" / "昨天没用，还记得为什么吗"（流失原因比留存原因值钱）。
- "那只猫对你意味着什么？没有猫你第二天还会打开吗"（探 hook 真伪）。
- "用的时候有没有想关掉/有点尬的瞬间？具体哪一步"（问瞬间不问觉得）。
- "它跟墨墨/不背/Anki比，你会为它放弃哪个"（探替代意愿）。
- "你预计一周后还会用吗？为什么"（预期可对账 D7）。
- "推荐给考研同学你会怎么介绍？介绍不出口卡在哪"（转述不出=没价值）。
- 收尾豁免权："这是一个朋友做的，他没说要怎么改进，就要大实话。你觉得最烂的一点是什么"（第三方归因+问最烂）。

**真话渠道**：①第三方归因法（口径统一"一个朋友做的，我帮他收集反馈"）②匿名表单（腾讯问卷/金数据关登录）③行为观察>口头（2-3人 think-aloud 30分钟，说"挺好的"打5折）④截图作证（第3/7天晒猫）⑤把"不回答"也当数据（48h没注册/约不出访谈=兴趣不足）⑥交叉对账（嘴上说爱、行为没回访的，以行为为准）。

**时间线（14天）**：D1-2招募 → D2-3发放+一对一onboarding → D3-13留存观察（每天跑一条SQL自动得D1/D3/D7）→ D5&D10两轮访谈+晒猫截图 → D14复盘决策。

**核心约束（来自代码事实）**：①服务端只有 cards 表有行为数据，猫亲密度本地不上云→次留按 user_id 看 cards 行去重，猫 hook 靠截图+访谈验证；②需邮箱注册→"注册"是激活漏斗第一步也是统计前提；③local-first 断网可用→要提醒用户"登录后数据才上云、才计入测试"。
